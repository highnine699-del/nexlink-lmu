lmu_portal.cred
nexlink_pro_license.cred
nexlink_theme.txt
lmu_autoconnect.log
*.log
*.cred
bin/
obj/
ecdsa_private_key.json

# Deployment guide contains private key values — never commit
DEPLOYMENT_GUIDE.md

# Lighthouse reports and other local audit artifacts
lighthouse.pdf

# Compiled binaries - distributed via GitHub Releases, not tracked in source
NexLink.exe
NexLink-Installer.exe
LMU-Auto-Connect-Installer.exe
LMU-AutoConnect.exe

# RSA developer keypair for credential encryption in error reports
# Private key must NEVER be committed — keep it only on your machine
rsa_private_key.xml

# PNG source files used by the old generate_icon.py script (now deleted)
# The .ico file is what the build pipeline uses; these serve no active purpose
wifi_16.png
wifi_32.png
wifi_48.png
wifi_256.png